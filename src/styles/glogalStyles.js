import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	flex: {
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'center',
	},
	shadow: {
		shadowColor: 'black',
		elevation: 8,
	},
});
